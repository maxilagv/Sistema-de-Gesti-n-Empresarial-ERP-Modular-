## PROD profile (placeholder - configure real DB and secrets)

# Example: PostgreSQL configuration (uncomment and set values)
# spring.datasource.url=jdbc:postgresql://host:5432/erpdb
# spring.datasource.username=erp
# spring.datasource.password=${DB_PASSWORD}
# spring.jpa.hibernate.ddl-auto=validate

# Flyway (recommended)
# spring.flyway.enabled=true

# JWT secret should come from env var in prod
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=3600000

# CORS should be restricted in prod
# management.endpoints.web.exposure.include=health,info

